# 指定环境
spring:
  profiles:
    active: local

# 以下部分为各个环境不同的配置
---

# 本地开发环境
spring:
  profiles: local

---

# 开发环境
spring:
  profiles: dev

---

# 测试环境
spring:
  profiles: test

---

# 生产环境
spring:
  profiles: prod

---